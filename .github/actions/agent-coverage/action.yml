name: Agent Scenario Coverage
description: Fail CI when expected Agent tool paths are not represented by recorded traces.
inputs:
  trace-dir:
    description: Directory containing recorded *.trace.json scenario files.
    required: true
  expected-paths:
    description: Comma-separated expected paths, for example get_order->cancel_order,get_order.
    required: true
  branch-paths:
    description: Comma-separated structured claim paths for business branch coverage.
    required: false
    default: ""
  expected-branches:
    description: Comma-separated expected scalar business branch values.
    required: false
    default: ""
  include-outcomes:
    description: Annotate tool paths with [ok] and [error] outcomes.
    required: false
    default: "false"
  report:
    description: Path for the JUnit coverage report.
    required: false
    default: outputs/agent-coverage.junit.xml
  summary:
    description: Path for the Markdown coverage summary.
    required: false
    default: outputs/agent-coverage.md
runs:
  using: composite
  steps:
    - name: Check Agent scenario coverage
      shell: bash
      env:
        TRACE_DIR: ${{ inputs.trace-dir }}
        EXPECTED_PATHS: ${{ inputs.expected-paths }}
        BRANCH_PATHS: ${{ inputs.branch-paths }}
        EXPECTED_BRANCHES: ${{ inputs.expected-branches }}
        INCLUDE_OUTCOMES: ${{ inputs.include-outcomes }}
        REPORT: ${{ inputs.report }}
        SUMMARY: ${{ inputs.summary }}
      run: |
        set -euo pipefail
        mkdir -p "$(dirname "$REPORT")" "$(dirname "$SUMMARY")"
        args=(coverage --trace-dir "$TRACE_DIR")
        IFS=',' read -r -a paths <<< "$EXPECTED_PATHS"
        for path in "${paths[@]}"; do
          args+=(--expected-path "$path")
        done
        if [[ "$INCLUDE_OUTCOMES" == "true" ]]; then
          args+=(--include-outcomes)
        fi
        if [[ -n "$BRANCH_PATHS" ]]; then
          IFS=',' read -r -a branch_paths <<< "$BRANCH_PATHS"
          for path in "${branch_paths[@]}"; do
            args+=(--branch-path "$path")
          done
        fi
        if [[ -n "$EXPECTED_BRANCHES" ]]; then
          IFS=',' read -r -a branches <<< "$EXPECTED_BRANCHES"
          for branch in "${branches[@]}"; do
            args+=(--expected-branch "$branch")
          done
        fi
        set +e
        agent-regression "${args[@]}" --format junit --out "$REPORT"
        coverage_status=$?
        agent-regression "${args[@]}" --format markdown --out "$SUMMARY"
        markdown_status=$?
        set -e
        if [[ -n "${GITHUB_STEP_SUMMARY:-}" && -f "$SUMMARY" ]]; then
          cat "$SUMMARY" >> "$GITHUB_STEP_SUMMARY"
        fi
        if [[ "$coverage_status" -ne 0 ]]; then
          exit "$coverage_status"
        fi
        exit "$markdown_status"
